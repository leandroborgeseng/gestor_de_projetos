# ============================================
# CONFIGURAÇÕES DE PRODUÇÃO
# ============================================
# Copie este conteúdo para .env.production e ajuste conforme necessário

# ============================================
# BANCO DE DADOS
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ALTERE_AQUI_GERE_UMA_SENHA_FORTE
POSTGRES_DB=agilepm
POSTGRES_PORT=5432

# ============================================
# API BACKEND
# ============================================
# URL de conexão com o banco (substitua ALTERE_AQUI pela senha acima)
DATABASE_URL=postgresql://postgres:ALTERE_AQUI_GERE_UMA_SENHA_FORTE@db:5432/agilepm

# Secrets JWT - IMPORTANTE: Use valores fortes e únicos!
JWT_SECRET=t5J7i23t+umEbGcKVOL0JjJ6WESXDjzz8BcP1MeEqFY=
JWT_REFRESH_SECRET=m2SWQqhDce/qjVmKl9X1iiyeCT1go0mdfEnjibxgFo8=

# Porta da API
API_PORT=4000

# URL do frontend (para CORS e links)
# Substitua SEU_IP_OU_DOMINIO pelo IP do seu servidor ou domínio
FRONTEND_URL=http://SEU_IP_OU_DOMINIO
# Exemplo com domínio: FRONTEND_URL=https://agilepm.exemplo.com
# Exemplo com IP: FRONTEND_URL=http://192.168.1.100

# ============================================
# FRONTEND WEB
# ============================================
# URL da API (usada pelo frontend)
# Substitua SEU_IP_OU_DOMINIO pelo IP do seu servidor ou domínio
VITE_API_URL=http://SEU_IP_OU_DOMINIO/api
# Exemplo com domínio: VITE_API_URL=https://agilepm.exemplo.com/api
# Exemplo com IP: VITE_API_URL=http://192.168.1.100/api

# Porta do frontend
WEB_PORT=80

# ============================================
# NGINX REVERSE PROXY (Opcional)
# ============================================
NGINX_PORT=8080

# ============================================
# INSTRUÇÕES
# ============================================
# 1. Substitua ALTERE_AQUI_GERE_UMA_SENHA_FORTE por uma senha forte
#    Gere com: openssl rand -base64 32
# 
# 2. Substitua SEU_IP_OU_DOMINIO pelo IP do seu servidor
#    Descubra o IP com: hostname -I | awk '{print $1}'
#    Ou use seu domínio se tiver configurado
#
# 3. Os JWT_SECRET e JWT_REFRESH_SECRET já estão gerados e prontos para uso
#    Mas você pode gerar novos com: openssl rand -base64 32
#
# 4. Salve este arquivo como .env.production (sem a extensão .template)

